apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
name: scheduler
nodes:
  - role: control-plane
    labels:
      node.id: node0
  - role: worker
    labels:
      node.id: node1
  - role: worker
    labels:
      node.id: node2
  - role: worker
    labels:
      node.id: node3

